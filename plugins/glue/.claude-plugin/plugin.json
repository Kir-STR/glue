{
  "name": "glue",
  "version": "0.3.0",
  "description": "Glue: native delivery of modular project rules (single plugin, embedded content). Commands: /glue:list, /glue:init, /glue:adopt (semantic init over existing rules), /glue:status; a SessionStart hook injects fallback guardrails until init."
}
